// Seed script placeholder — will be populated with realistic KGG data
// Run with: npm run db:seed

async function main() {
  // TODO: Import PrismaClient and seed experts, clients, projects, engagements
  // Based on 274 real projects across 15 categories
  console.log("Seed script placeholder — connect Prisma after PostgreSQL is running");
}

main().catch(console.error);
